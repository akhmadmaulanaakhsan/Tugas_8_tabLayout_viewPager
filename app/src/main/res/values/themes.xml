<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Tugas8TabLayoutViewPager" parent="Theme.AppCompat.DayNight">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="titleTextColor">@color/abu</item>

        <!-- Uncomment the following line if you want to use your_light_primary -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->

        <!-- Add the following line to enable ActionBar -->
        <item name="windowActionBar">true</item>
        <item name="windowNoTitle">false</item>
    </style>

    <style name="Theme.Tugas8TabLayoutViewPager" parent="Base.Theme.Tugas8TabLayoutViewPager" />
</resources>
